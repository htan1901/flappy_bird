import 'package:flutter/material.dart';

class Barier extends StatelessWidget {
  const Barier({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}